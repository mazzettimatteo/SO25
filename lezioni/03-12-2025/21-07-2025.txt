Scrivere il monitor nmeet che ha una funzione di interfaccia:
float nmeet(unsigned int n, float val)
La funzione nmeet blocca il processo chiamante fino a quando n processi abbiano richiamato nmeet con parametro n.
(es due processi che hanno chiamato la nmeet con n=3 rimangono bloccati, quando un terzo processo chiama nmeet
con n=3, i tre processi continuano l'esecuzione.)
 Il valore di ritorno di nmeet e' la media dei valori 'val' passati dagli n processi che hanno sbloccato la chiamata nmeet.
CI VUOLE UNA CONDIZIONE PER OGNI VAL DI n
Problema Sync: fai in modo che i primi n-1 si fermino e l'n-esimo li faccia partire tutti
Problema 2: fare la media di tutti gli n
pe=procedure entry

monitor nmeet
waitingArr:=new dynamic array (int nwait, condition c) //simile ai dict python
pe float nmeet(unsigned int n, float val)
	//ogni richiesta ho 2 casi: se i processi sono < n bisogna aspettare, se arriva l'n-esimo bisogna risvegliarli
	if(n not in waitingArr)
		waitingArr[n].nwait=0
		//new(waitingArr[n])
	waitingArr[n].nwait++;
	if(waitingArr[n].nwait==n)
		waitingArr[n].c.signal()
	else 
		waiuìtingArr[n].c.wait()
		waitingArr[n].c.signal()
	waitingArr[n].nwait--

arriva il primo: non c'è l'elem di indice 3 e inizializza a 0 e poi incrementa di 1, 1!=3 (n=3) e si ferma sulla cond
arriva 1: si ferma in riga waitingArr[n].cwait()
arriva un altro 3: si blocca
arriva un terzo 3: si ferma a riga waitingArr[n].c.signal() 

adesso aggiungiamo un calcolatore di media:

monitor nmeet
waitingArr:=new dynamic array (int nwait, condition c, float sum) //simile ai dict python
float avg=0 //media
pe float nmeet(unsigned int n, float val)
	//ogni richiesta ho 2 casi: se i processi sono < n bisogna aspettare, se arriva l'n-esimo bisogna risvegliarli
	if(n not in waitingArr)
		waitingArr[n].nwait=0
		waitingArr[n].sum=0
		//new(waitingArr[n])
	waitingArr[n].nwait++;
	waitingArr[n].sum+=val
	if(waitingArr[n].nwait==n)
		waitingArr[n].c.signal()
		avg=waitingArr[n].sum/n
	else 
		waiuìtingArr[n].c.wait()
		waitingArr[n].c.signal()
	waitingArr[n].nwait--
	return avg






